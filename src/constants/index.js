/**
 * Constants Module Aggregator
 * Centralizes all constants exports
 */
const { HTTP_STATUS, HTTP_MESSAGES } = require('./http.constants');

module.exports = {
    HTTP_STATUS,
    HTTP_MESSAGES,
};
